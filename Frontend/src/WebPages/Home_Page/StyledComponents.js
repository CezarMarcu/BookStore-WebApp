import styled from "styled-components";

export const ContentContainer = styled.div`
    background:gray;
    width: auto;
    height: 30rem;
    position: absolute;
    display: flex;
    flex-direction: column;
`